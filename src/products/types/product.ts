export type DatabaseProduct = {
  id: string;
  description: string;
  price: number;
  imageurl: string;
  stock: number;
};

export type Product = {
  id: string;
  description: string;
  price: number;
  imageUrl: string;
  stock: number;
};
